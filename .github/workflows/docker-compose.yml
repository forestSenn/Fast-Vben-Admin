name: Docker Compose

on:
  pull_request:
  push:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Prepare environment
        run: cp .env.example .env

      - name: Validate compose file
        run: docker compose -f compose.yml config

      - name: Build images
        run: docker compose -f compose.yml build

      - name: Start infrastructure
        run: docker compose -f compose.yml up -d --no-build db mailcatcher adminer

      - name: Run prestart
        run: docker compose -f compose.yml up --no-build prestart

      - name: Start application
        run: docker compose -f compose.yml up -d --no-build backend frontend

      - name: Wait for backend health
        run: |
          for i in $(seq 1 60); do
            if curl -fsS http://localhost:8000/api/v1/utils/health-check; then
              exit 0
            fi
            sleep 2
          done
          docker compose -f compose.yml logs
          exit 1

      - name: Show compose logs on failure
        if: failure()
        run: |
          docker compose -f compose.yml ps -a
          docker compose -f compose.yml logs --no-color

      - name: Stop services
        if: always()
        run: docker compose -f compose.yml down -v
